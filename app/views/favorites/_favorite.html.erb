<% if !current_user?(offer.user) %>
<!-- current_user -->
   <span class="kininaru">
     <% if offer.kininaru?(current_user) %>
      <%= form_for(offer.favorites.find_by(user_id: current_user.id), method: :delete, remote:true) do |f| %>
        <%= button_tag(class: "btn btn-default btn-xs") do %>
          <%= content_tag :span, "#", class: "glyphicon glyphicon-heart-empty" %>
        <% end %>
      <% end %>
    <% end %>
  </span>
<% end %>
